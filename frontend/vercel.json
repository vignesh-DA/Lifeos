{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://<YOUR_RENDER_URL>/api/:path*"
    },
    {
      "source": "/auth/:path*",
      "destination": "https://<YOUR_RENDER_URL>/auth/:path*"
    }
  ]
}
